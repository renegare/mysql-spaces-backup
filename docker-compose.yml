version: '3'
services:
  backup:
    build: .
  db:
    image: mysql:5.7
    environment:
      MYSQL_ROOT_PASSWORD: password
      MYSQL_DATABASE: database_dmp
    ports:
    - "3307:3306"
    volumes:
    - ./datadir:/var/lib/mysql
